# Application
NODE_ENV=development

# URL Shortener
# Use port 5173 for development, 3000 for production (docker)
PUBLIC_URL=http://localhost:5173

# Local development database (docker compose up -d db exposes it on 55432)
DATABASE_URL=postgresql://shortener:shortener@localhost:55432/shortener
