# The web server bundle keeps @prisma/client external (it resolves generated
# code at require-time), so it must be resolvable from applications/web at
# runtime even though it's a dependency of libs/infrastructure. Hoisting it
# to the root node_modules is the documented pnpm escape hatch for this.
public-hoist-pattern[]=*prisma*
